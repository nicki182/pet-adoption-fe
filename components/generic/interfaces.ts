export type SizeTypes =
  | 'small'
  | 'medium'
  | 'big'
  | 'default'
  | 'none'
  | 'huge';
export type LenguageType = 'us';
export interface Position {
  top: string;
  left: string;
  right: string;
  bottom: string;
}
